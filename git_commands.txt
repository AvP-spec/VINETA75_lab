git status -s # short status
git add .     # add evrething, or put filename instead of dot
git log --oneline --reverse
git commit --amend --no-edit  # extend previouse commit 
git diff file_name.py
git difftool file_name.py

git pull origin main --allow-unrelated-histories
git push -u origin main
git log --oneline --graph --decorate -n 5

git remote -v # list all remotes
git fetch origin
git diff main Lab_avp/main --stat

git diff --staged devices/arduino_AvP.py
git diff --name-status origin/main  ## see modified file names

git rm git rm path/to/file

#########################################################
################### fetch from Gitea ##################
# Download all history and branches from remote without changing local files
git fetch origin 

git checkout -b review/remote-updates origin/main

# List all files that are different A-new, M-modified, D-deleted:
git diff --name-status main..HEAD

# List only new files (A) 
git diff --name-only --diff-filter=A main origin/main
# Copy all new files from origin/main to the active branch.
git checkout origin/main -- $(git diff --name-only --diff-filter=A main origin/main)
git add . 
git commit -m "new files added from the remote"
git status

git checkout main -- path/to/file.ext
git difftool origin/main -- path/to/file.ext
git add . 
git commit -m "file name"
# see modified files
git log --oneline main..HEAD
git log --oneline --graph main..HEAD

# switch to main
git checkout main
# merge chenges
git merge review/remote-updates
# in case of resolved fconflicts 
git commit -m ""
# delete the branch
git branch -d review/remote-updates
## force to delit ####
git branch -D review/remote-updates 


git reset --soft origin/main

git difftool origin/main -- local_path/file
#########################################################



#############################################################
##### get Eriks updates ##### fetch from Gitea ##################
git fetch origin Eriks-branch:Eriks-branch

## make and got to the review baranch on the base of Ericks-branch
git checkout -b review origin/Eriks-branch

## go back to main 
git checkout main
## List only files A (A-new, M-modified, D-deleted)
git diff --name-only --diff-filter=A main review
## Copy all new files from Eriks-branch to the active branch.
git checkout Eriks-branch -- $(git diff --name-only --diff-filter=A main Eriks-branch)
## save the changes 
git add . 
git commit -m "new files added"
 
git diff --name-only --diff-filter=M main review
## take file SpyderFiles/LIF-piezo_scan.py from review
git checkout review -- SpyderFiles/LIF-piezo_scan.py

## go to review
git switch review
git checkout main -- git_commands.txt
## see files in main which are not in review
git diff --name-only --diff-filter=A review main
git diff --name-only review main

git difftool main -- devices/base_device.py  # compare the files with version from main

git switch main                             # go to main
## shouold be something else ## git merge --squash review                    # collect all commits in one and merge to main
git commit -m ""                             # save changes in main
git branch -D review                         # delit branch review

#########################################################

################################################
################# Look history ###################
## Safely view file differences between local HEAD and origin/main in VS Code without modifying working directory files.
git difftool HEAD origin/main -- path/to/file.ext

## copy lif.py file from origin/main to main with name lif_avp.py
git show origin/main:managers/lif.py > managers/lif_avp.py

## compare file lif.py from commit af74b11 with file if_avp.py
git difftool af74b11:managers/lif.py managers/lif_avp.py


### take file devices/Perkin_Elmer_DS7780.py to the main from Erick-branch with new name
git show origin/Eriks-branch:devices/Perkin_Elmer_DS7780.py > devices/Perkin_Elmer_DS7780_erick.py









################### push to Gitea ##################

#### look and clean history
git log --oneline --reverse

## set all changes from origin/main as one commit
### WAY 1 ==================================
git reset --soft origin/main
git commit -m 'massage'
### WAY 2 ====================================
# to collect last 10 commits in one
git rebase -i HEAD~10
## alternative 
git rebase -i origin/main
# in redactor let first "pick" others chahge to "squash" or "s"
# save and close
# new reductor window: write new comment 
## ===============================================

### look if Nextcloud folder has changes
git fetch Lab_avp
#### pull from Nextcloud 
cd ~/Nextcloud/5360.AG_Manz/Programming/python/Lab_avp 
git pull office main    # git pull (remote) (branch)

# synchonization with main folder 
cd /c/Andrei/LAB
git fetch Lab_avp

### push to Gitea
git push origin main



###########################################################
##### Git configuration ################

## Windoes lokal account! for isolation privat keys and correct identification

## user information
git config --global user.name andrei_uni
git config --global user.email avpipa@gmail.com

## for windows true, for linux input
git config --global core.autocrlf true
## set name "main" (old version is "master")
git config --global init.defaultBranch main
## use VS code as editor
git config --global core.editor "code --wait"
git config --global merge.tool vscode
git config --global difftool.vscode.cmd "code --wait --diff \$LOCAL \$REMOTE"
git config --global merge.tool vscode
git config --global mergetool.vscode.cmd "code --wait --merge \$REMOTE \$LOCAL \$BASE \$MERGED"


## make file
.gitignore
------------------
__pycache__/
.*
!.gitignore
------------------


## remote connection
ls -al ~/.ssh                        # see available keys
mkdir -p ~/.ssh                      # make folders for keys
ssh-keygen -t ed25519 -f ~/.ssh/key1 # generate key in file key1 
cat ~/.ssh/key1.pub                  # see the key
git remote set-url origin ssh_remote name  ## set remote via ssh
git remote -v                        # check remote settings

eval `ssh-agent -s`                  # run Windows ssh agent in this terminal
ssh-add ~/.ssh/key1                  # add the key to the agent


##### first commit ##############
git init  ## take the folder ander control of git
git add . ## add whole directory
git status ## see staged files
git reset ## cancel git add .
git commit -m "Initial commit"