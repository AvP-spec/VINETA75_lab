git status -s # short status
git add .     # add evrething, or put filename instead of dot
git log --oneline --reverse
git commit --amend --no-edit  # extend previouse commit 
git diff file_name.py
git difftool file_name.py

git pull origin main --allow-unrelated-histories
git push -u origin main

###########################################################
##### Git configuration ################

## Windoes lokal account! for isolation privat keys and correct identification

## user information
git config --global user.name andrei_uni
git config --global user.email avpipa@gmail.com

## for windows true, for linux input
git config --global core.autocrlf true
## set name "main" (old version is "master")
git config --global init.defaultBranch main
## use VS code as editor
git config --global core.editor "code --wait"
git config --global merge.tool vscode
git config --global difftool.vscode.cmd "code --wait --diff \$LOCAL \$REMOTE"
git config --global merge.tool vscode
git config --global mergetool.vscode.cmd "code --wait --merge \$REMOTE \$LOCAL \$BASE \$MERGED"


## make file
.gitignore
------------------
__pycache__/
.*
!.gitignore
------------------

##### first commit ##############
git init  ## take the folder ander control of git
git add . ## add whole directory
git status ## see staged files
git reset ## cancel git add .
git commit -m "Initial commit"